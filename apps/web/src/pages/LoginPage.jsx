export default function LoginPage() {
  return (
    <section className="card">
      <h2>Login</h2>
      <p>Route shell ready. Connect this page to POST /api/auth/login in the next step.</p>
    </section>
  );
}
